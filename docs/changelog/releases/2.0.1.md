# Release 2.0.1

Release status: `in preparation`

Release date target: `2026-04-08`

This file keeps the implementation history and release notes for the current incremental release inside the release 2 cycle.

## Release Summary

### Current preparation scope

- Continued the release 2 preparation flow by incrementing the working milestone from `2.0.0` to `2.0.1` as new polish work lands.
- Kept the release 2 notes focused on admin approval UX refinements in the web client.

### `current` - Refine pending approval feedback and modal action styling

- Kept the web `Pending approvals` approval flow aligned with the release 2 feedback improvements, including the temporary approval notification and action-specific processing states.
- Updated the temporary-password modal on the web `Pending approvals` page so the `Close` action now uses the same black primary-button styling as the rest of the admin approval flow.
